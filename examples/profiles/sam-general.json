{
  "$schema": "../../../schema/shift-profile-v1.json",
  "schemaVersion": 1,
  "person": "sam",
  "set": "general",
  "name": "Sam general",
  "description": "Operator-only map. Person-role files for gamepad 2 must declare controllers.codriver.",
  "controllers": {
    "codriver": {
      "slot": 2,
      "defaultLayer": "mechanism"
    }
  },
  "layers": {
    "mechanism": {
      "controller": "codriver",
      "bindings": [
        {
          "source": "A",
          "event": "WHILE_HELD",
          "intent": "intake.collect"
        }
      ]
    }
  }
}
